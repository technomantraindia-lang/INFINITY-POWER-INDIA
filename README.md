# Infinity Power India homepage

Open `index.html` in a browser to view the responsive homepage. The page is
built from HTML, CSS, and JavaScript; `home page.png` remains only as the
design reference and is not loaded by the website. The bespoke wide hero asset
is stored in `assets/hero-banner-reference.png`.
